/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {
    val s = readLine() //realiza a leitura de inputs no terminal
    
    if (s != null && s !=""){
        s.toInt()
        s.toFloat()
        s.toBoolean()
    }else{
        println("oi")
    }
}